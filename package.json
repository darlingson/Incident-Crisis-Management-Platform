{
  "name": "incident-crisis-platform",
  "private": true,
  "version": "0.1.0",
  "description": "Incident & Crisis Management Platform — Api (.NET 10) + web (Next 16.3.4)",
  "workspaces": ["web"],
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"dotnet run --project Api/Api.csproj\" \"npm run dev --workspace=web\"",
    "dev:api": "dotnet run --project Api/Api.csproj",
    "dev:web": "npm run dev --workspace=web",
    "build": "concurrently -n api,web \"dotnet build Api/Api.csproj\" \"npm run build --workspace=web\"",
    "test": "dotnet test Api.Tests/Api.Tests.csproj",
    "lint": "npm run lint --workspace=web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20.9.0",
    "npm": ">=10"
  }
}
